version: '3.8'

services:
  training:
    image: projeto-final-rl:latest
    command: python main.py
    volumes:
      - ./main.py:/app/main.py
      - ./replay.py:/app/replay.py
      - ./summary.py:/app/summary.py
      - ./compare.py:/app/compare.py
      - ./smpe.py:/app/smpe.py
      - ./src:/app/src
      - ./configs:/app/configs
      - ./checkpoints:/app/checkpoints
      - ./progress:/app/progress
      - ./results:/app/results
      - ./models:/app/models
      - ./videos:/app/videos
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  replay:
    image: projeto-final-rl:latest
    command: python replay.py
    volumes:
      - ./main.py:/app/main.py
      - ./replay.py:/app/replay.py
      - ./summary.py:/app/summary.py
      - ./compare.py:/app/compare.py
      - ./src:/app/src
      - ./configs:/app/configs
      - ./checkpoints:/app/checkpoints
      - ./progress:/app/progress
      - ./results:/app/results
      - ./models:/app/models
      - ./videos:/app/videos
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
