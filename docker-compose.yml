services:
  training:
    image: projeto-final-rl:latest
    command: python main.py
    volumes:
      - ./main.py:/app/main.py
      - ./src:/app/src
      - ./configs:/app/configs
      - ./progress:/app/progress
      - ./results:/app/results
      - ./videos:/app/videos
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]

  replay:
    image: projeto-final-rl:latest
    command: python replay.py
    volumes:
      - ./replay.py:/app/replay.py
      - ./smpe.py:/app/smpe.py
      - ./src:/app/src
      - ./configs:/app/configs
      - ./progress:/app/progress
      - ./results:/app/results
      - ./videos:/app/videos
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
  run_smpe:
    image: projeto-final-rl:latest
    command: python smpe.py
    volumes:
      - ./smpe.py:/app/smpe.py
      - ./progress:/app/progress
      - ./results:/app/results
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
